apiVersion: apps/v1
kind: Deployment
metadata:
  name: hive-app
spec:
  template:
    spec:
      containers:
        - name: hive-app
          env:
            - name: HIVE_DEMO_DATA_ENABLED
              value: "true"
            - name: HIVE_SQL_INIT_MODE
              value: always
