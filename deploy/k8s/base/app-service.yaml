apiVersion: v1
kind: Service
metadata:
  name: hive-app
  labels:
    app.kubernetes.io/name: hive
    app.kubernetes.io/component: app
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: hive
    app.kubernetes.io/component: app
  ports:
    - name: http
      port: 8080
      targetPort: http
